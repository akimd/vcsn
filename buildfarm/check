#!/bin/sh

set -e
. $(dirname "$0")/common

make_()
{
  make DESTDIR="$DIR_INSTALL" "$@"
}

check()
{
  make check -j${NBPROC:-2} "$@"
}

## ------ ##
## Main.  ##
## ------ ##

cd "$DIR_BUILD"
check
