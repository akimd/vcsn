# Test cases independant of the weightsets.

%labels: letters
%include: common.rat
# Base cases.
ab => a.b
# Error cases.
x ! x: 1.1: invalid letter: x

# Precedence and associativity.
(ab)* => (a.b)*
abc => a.b.c
a(bc) == a.b.c
(abc).(cba) => a.b.c.c.b.a


%labels: words
%include: common.rat
# Base cases.
ab => ab
# Error cases.
x ! x: 1.1: invalid word: x: invalid letter: x
# Yes, the message is not as nice as one might expect, because
# when reading a ratexp, we actually read letter by letter, and
# only afterwards this is turned into a word, by concatenation.
# So first we see that 'x' is a not a valid letter.
axb ! axb: 1.2: invalid word: x: invalid letter: x

# Precedence and associativity.
(ab)* => (ab)*
abc => abc
a(bc) => a.(bc)
ab(ab) => (ab).(ab)
(ab)ab => (ab).(ab)
ab(ab)ab => (ab).(ab).(ab)
ab(ab)abc*  => (ab).(ab).(ab).(c*)
(abc).(cba) => (abc).(cba)
